export class CodeLabel {
    code: string;
    label: string;
    src: string;

    constructor(code: string, label: string, src: string) {
        this.code = code;
        this.label = label;
        this.src = src;
    }
}
