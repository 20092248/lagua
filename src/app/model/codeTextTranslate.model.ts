export class CodeTextTranslate {
    code: string;
    text: string;
    translate: string;

    constructor(code: string, text: string, translate: string) {
        this.code = code;
        this.text = text;
        this.translate = translate;
    }
}
